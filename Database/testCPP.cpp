#include <cstdlib>
#include <string>

int main() {
	/*
    std::string name = "Mubazir";
    int age = 19;
    std::string command = "python database.py " + name + " " + std::to_string(age);
	system(command.c_str());
	*/
    system("python testPY.py");
    return 0;
}
